{
  "name": "adm-taric-wiki",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build:unix": "sudo astro build && sudo touch docs/.nojekyll",
    "build:win": "astro build && echo >> docs/.nojekyll",
    "build:specific": "sudo mkdir -p temp && cp -r /Users/vincenzoingenito/Desktop/FORK/it-fse-support/doc/wiki/* temp/ && sudo astro build temp && rm -rf temp",
    "astro": "astro",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/check": "^0.5.10",
    "@astrojs/starlight": "^0.21.5",
    "astro": "^4.3.5",
    "sharp": "^0.32.5",
    "shiki": "^1.6.0",
    "typescript": "^5.4.5"
  }
}
